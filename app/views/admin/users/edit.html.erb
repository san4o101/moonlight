
<div class="card">
  <div class="card-header">
    Edit User
  </div>
  <div class="card-body">
    <%= render 'form', user: @user, model_url: admin_user_path(@user) %>

    <%= link_to 'Show', admin_user_path(@user), class: 'btn btn-primary' %> |
    <%= link_to 'Back', admin_users_path, class: 'btn btn-secondary' %>
  </div>
</div>
