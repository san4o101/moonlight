<%= render('admin/settings/nav') %>

<div class="card">
  <div class="card-header">
    Edit country
  </div>
  <div class="card-body">
    <%= render 'form', country: @country, model_url: edit_admin_settings_country_path(@country) %>

    <%= link_to 'Show', admin_settings_country_path(@country), class: 'btn btn-primary' %> |
    <%= link_to 'Back', admin_settings_countries_path, class: 'btn btn-secondary' %>
  </div>
</div>
