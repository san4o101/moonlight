<% if notification.status == ManagerNotification::ACTIVE_STATUS %>
  <span class="badge badge-success noselect"><%= t('notifications.types.active_one') %></span>
<% elsif notification.status == ManagerNotification::VIEWED_STATUS %>
  <span class="badge badge-secondary noselect"><%= t('notifications.types.viewed_one') %></span>
<% end %>