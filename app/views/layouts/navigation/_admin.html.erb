<nav class="navbar navbar-expand-lg bg-dark navbar-dark bg-light rounded-bottom mb-4 shadow">
  <a class="navbar-brand" href="<%= admin_home_path %>">
    <img src="/images/moon_logo.svg" width="25" height="25" class="d-inline-block align-top" alt="">
    Moonlight
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link <%= current_page?(admin_users_path) ? ' active ' : '' %>"
           href="<%= admin_users_path %>">
          <%= t('user.field.users') %>
        </a>
      </li>
      <%= render('layouts/navigation/user_info') %>
    </ul>
  </div>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= pundit_user.full_name %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to admin_settings_countries_path, method: :get, class: 'dropdown-item' do %>
              <i class="fa fa-cog"></i> <%= t('action.settings') %>
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete, class: 'dropdown-item' do %>
              <i class="fa fa-sign-out"></i> <%= t('action.logOut') %>
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<%= render('layouts/navigation/breadcrumbs') %>